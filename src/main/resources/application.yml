server:
  port: 8080

spring:
  threads:
    virtual:
      enabled: true
  application:
    name: microservice-customer
  
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/rdb-microservice-customer}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

  config:
    import: "optional:consul:"
  cloud:
    function:
      autodetect: false
    stream:
      function:
        autodetect: false
        definition: ""
      kafka:
        binder:
          brokers: ${KAFKA_BROKERS:localhost:19092}
          configuration:
            schema.registry.url: ${SCHEMA_REGISTRY_URL:http://localhost:8081}
        bindings:
          customerEvents-in-0:
            consumer:
              enableDlq: true
              dlqName: customer.dlq
      bindings:
        customerEvents-out-0:
          destination: customer
          content-type: application/*+avro
        customerEvents-in-0:
          destination: customer
          group: ${spring.application.name}-group
          content-type: application/*+avro
          consumer:
            max-attempts: 3
            back-off-initial-interval: 1000
            back-off-max-interval: 10000
            back-off-multiplier: 2.0
    consul:
      host: ${CONSUL_HOST:localhost}
      port: 8500
      discovery:
        service-name: microservice-customer
        instance-id: ${spring.application.name}:${server.port}
